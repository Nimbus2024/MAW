# torch/torchvision 版本由服务器 GPU/CUDA 决定(如 A800 cu126/cu128), 不在此锁定;
# 建议用 setup_env.sh --torch cuXXX 单独装。其余核心库锁到参考生产(A100/opsd)实测版本。
torch
torchvision
transformers==4.57.1
accelerate==1.11.0
peft==0.17.1
datasets==5.0.1
bitsandbytes
pandas
pyarrow
Pillow
nltk
rouge-score
scikit-learn
tqdm
tensorboard
sentencepiece
# vllm 不放入基础依赖: 版本需与 CUDA/torch 匹配(曾用 0.11.0), 用 setup_env.sh --vllm [VER] 单独安装
# vllm
